#!/bin/bash

declare color='\e[93m'
declare wipe="\033[1m\033[0m"

cat $HOME/.bash.d/bashrc >> $HOME/.bashrc
echo -e "Settings have been saved to your ${color}.bashrc${wipe} file."

if [ ! -f $HOME/.inputrc ]; then
    ln -s $HOME/.bash.d/inputrc $HOME/.inputrc
fi
